# Environment Variables Example
# Copy this file to .env.dev or .env.prod and fill in actual values
# NEVER commit .env.dev or .env.prod to version control!

# ============================================
# Environment Type
# ============================================
ENV=dev

# ============================================
# API Configuration
# ============================================
API_BASE_URL=https://dev-api.example.com

# ============================================
# Firebase Configuration
# ============================================
# Get these values from your Firebase project settings
# or from google-services.json / GoogleService-Info.plist

FIREBASE_API_KEY=your_firebase_api_key_here
FIREBASE_APP_ID=your_firebase_app_id_here
FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id_here
FIREBASE_PROJECT_ID=your-firebase-project-id
FIREBASE_STORAGE_BUCKET=your-firebase-project.appspot.com

# ============================================
# Security & Encryption
# ============================================
# 32-character encryption key for secure storage
# Generate a strong random key for production!
ENCRYPTION_KEY=your_32_character_encryption_key

# ============================================
# Optional: Third-Party Services
# ============================================

# Google Maps API Key (if using maps)
GOOGLE_MAPS_API_KEY=your_google_maps_api_key

# Stripe Publishable Key (if using payments)
# Use test keys (pk_test_...) for development
# Use live keys (pk_live_...) for production
STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_key

# Sentry DSN (if using Sentry for error tracking)
SENTRY_DSN=https://your_sentry_dsn@sentry.io/project_id

# ============================================
# Notes
# ============================================
# 1. Copy this file:
#    - For development: cp .env.example .env.dev
#    - For production: cp .env.example .env.prod
#
# 2. Add to .gitignore:
#    .env.dev
#    .env.prod
#
# 3. Use in build scripts:
#    - Load variables: export $(cat .env.dev | xargs)
#    - Pass to Flutter: --dart-define=API_BASE_URL=$API_BASE_URL
#
# 4. SECURITY:
#    - NEVER commit actual credentials to git
#    - Use different keys for dev and prod
#    - Store production keys in CI/CD secrets
#    - Rotate keys regularly

